# Design System: Claude

> This is a real `DESIGN.md` example generated from [https://claude.com](https://claude.com) with `npx brandmd`.
>
> Drop a `DESIGN.md` like this in your project root so Claude Code, Cursor, Gemini CLI, Codex, or Google Stitch can use the colors, typography, spacing, and UI patterns when generating UI.
>
> Generate one for your site: `npx brandmd https://yoursite.com` ([npm](https://www.npmjs.com/package/brandmd) · [repo](https://github.com/yuvrajangadsingh/brandmd))
## 1. Visual Theme & Atmosphere

**Overall mood:** Dark and moody

**Density:** Moderate. The layout uses a varied spacing scale.

**Shape language:** Rounded, friendly aesthetic with generous corner radii.

**Depth:** Uses 5 shadow styles for layering and elevation.

## 2. Color Palette & Roles

- **Light Yellow** (`#FAF9F5`): Page background
- **Black** (`#141413`): Dark background / footer
- **Dark gray** (`#5E5D59`): Secondary background
- **Orange** (`#C6613F`): Accent background
- **Black** (`#000000`): Primary text
- **Muted Yellow** (`#B0AEA5`): Muted text
- **Light Muted Yellow** (`#D1CFC5`): Divider / border
- **Black** (`#30302E`): Divider / border

## 3. Typography Rules

**Primary font:** anthropicSans
**Secondary font:** anthropicSerif

**Fonts by role:**
- Headings: anthropicSans, anthropicSerif
- Body: anthropicSans

**All detected fonts:** anthropicSans (782), anthropicSerif (11), Segoe UI (1)

**Type scale:**
- Headings: 24px, 25px, 30px, 52px
- Body / UI: 14px, 15px, 16px, 17px
- Captions / Small: 11px, 12px

**Weights in use:** 330, 400, 500, 600, 700

**Line heights:** 22.5px, 18px, 26px, 24px, 19.55px, 17.25px, 28px, 36px, 20px, 16px

**Letter spacing:** 0.15px

## 4. Component Stylings

### Buttons
- Text color: `rgb(250, 249, 245)`
- Corner radius: 8px
- Padding: 8px 16px 8px 16px
- Font: 17px, weight 500

### Cards
- Padding: 0px 0px 0px 0px

### Inputs
- Border: 0px none rgb(250, 249, 245)
- Padding: 4px 0px 4px 0px
- Font size: 12px

## 5. Layout Principles

**Spacing scale:** 4px, 8px, 12px, 16px, 20px, 24px, 32px, 64px

**Base unit:** 4px grid (values are mostly multiples of 4).

**Border radii:** 4px, 8px, 9.6px, 12px, 12px 12px 0px 0px, 16px, 24px, 32px

## 6. Guidelines

### Do
- Use 4px grid for all spacing
- Use `#C6613F` for primary actions and CTAs
- Use `anthropicSans` as the primary typeface

### Don't
- Don't introduce colors outside the palette above
- Don't mix fonts beyond anthropicSans and anthropicSerif
- Don't use inline styles when the design system covers the pattern
- Don't use border-radius values outside: 4px, 8px, 9.6px, 12px, 12px 12px 0px 0px, 16px, 24px, 32px

---

*This DESIGN.md was generated by [brandmd](https://github.com/yuvrajangadsingh/brandmd). Drop it into your project root and AI coding agents (Claude Code, Cursor, Gemini CLI) will use it to generate on-brand UI.*