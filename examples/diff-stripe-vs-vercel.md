# Brand diff: Stripe vs Vercel

> Generated by `brandmd diff` from stripe.md and vercel.md.
>
> Both files are snapshots, not canonical guidelines.


> Generated by `brandmd diff` from stripe.md and vercel.md.
>
> Both files are snapshots, not canonical guidelines.

## Color palette

### Shared (0)

_none_

### Only in Stripe (7)

- `#FFFFFF` — background (background)
- `#000000` — on-background (on-background)
- `#64748D` — on-surface-variant (on-surface-variant)
- `#533AFD` — primary (primary)
- `#FFFFFF` — on-primary (on-primary)
- `#29227D` — secondary (secondary)
- `#FFFFFF` — on-secondary (on-secondary)

### Only in Agentic Infrastructure (10)

- `#FAFAFA` — background (background)
- `#4D4D4D` — on-background (on-background)
- `#00FF95` — surface (surface)
- `#8F8F8F` — on-surface-variant (on-surface-variant)
- `#EBEBEB` — outline (outline)
- `#00000014` — outline-variant (outline-variant)
- `#FF1744` — primary (primary)
- `#1A1A1A` — on-primary (on-primary)
- `#FFD000` — secondary (secondary)
- `#1A1A1A` — on-secondary (on-secondary)


## Typography

| | Stripe | Agentic Infrastructure |
|---|---|---|
| Primary font | sohne-var | GeistSans |
| Secondary font | _unspecified_ | _unspecified_ |
| Heading scale | 48px, 32px, 26px | 64px, 56px, 30px |
| Body scale | 16px, 14px | 16px, 14px |
| Weights | 300, 400 | 400, 450 |

## Spacing & radii

| | Stripe | Agentic Infrastructure |
|---|---|---|
| Spacing scale | 8px, 1px, 2px, 4px, 6px, 10px | 2px, 3px, 4px, 5px, 6px, 8px |
| Border radii | 4px, 5px, 6px, 8px, 16px | 2px, 6px, 8px, 9999px |

## Components

### Buttons

| property | Stripe | Agentic Infrastructure | |
|---|---|---|---|
| Background | #533afd | _unspecified_ | ≠ |
| Corner radius | 4px | 0px | ≠ |
| Font | 14px, weight 400 | 14px, weight 400 | = |
| Height | 40px | 32px | ≠ |
| Padding | 15.5px 20px 16.5px 20px | 0px 0px 0px 0px | ≠ |
| Text color | #ffffff | #4d4d4d | ≠ |

_5 of 6 properties differ between Stripe and Agentic Infrastructure buttons._


### Cards

_only in Stripe_:
```
Corner radius: 0px
Padding: 0px 0px 0px 0px
```

### button-primary

_only in Stripe_:
```
backgroundColor: #533afd
textColor: #ffffff
typography: {typography.label-sm}
rounded: {rounded.sm}
padding: 16px
height: 40px
```

### button-secondary

| property | Stripe | Agentic Infrastructure | |
|---|---|---|---|
| backgroundColor | transparent | transparent | = |
| height | 676px | 32px | ≠ |
| rounded | 0px | 0px | = |
| typography | {typography.label-sm} | {typography.label-sm} | = |

_1 of 4 properties differ between Stripe and Agentic Infrastructure button-secondary._


### card

_only in Stripe_:
```
rounded: 0px
```

## Synthesis

### What to copy

- Primary fonts diverge (`sohne-var` vs `GeistSans`), typography is the cleanest brand-signature swap.
- display font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- headline-lg font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- headline-md font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- body-md font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- body-lg font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- label-sm font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- headings font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans, Geist Mono`.
- body font differs: Stripe uses `sohne-var`, Agentic Infrastructure uses `GeistSans`.
- Button radius gap: Stripe uses `4px`, Agentic Infrastructure uses `0px`. Buttons are where users feel the difference fastest.

### What to avoid

_no obvious anti-pattern_

