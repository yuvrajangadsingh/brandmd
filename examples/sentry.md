# Design System: Application Performance Monitoring & Error Tracking Software | Sentry

> This is a real `DESIGN.md` example generated from [https://sentry.io](https://sentry.io) with `npx brandmd`.
>
> Drop a `DESIGN.md` like this in your project root so Claude Code, Cursor, Gemini CLI, Codex, or Google Stitch can use the colors, typography, spacing, and UI patterns when generating UI.
>
> Generate one for your site: `npx brandmd https://yoursite.com` ([npm](https://www.npmjs.com/package/brandmd) · [repo](https://github.com/yuvrajangadsingh/brandmd))
## 1. Visual Theme & Atmosphere

**Overall mood:** Balanced and professional

**Density:** Moderate. The layout uses a varied spacing scale.

**Shape language:** Rounded, friendly aesthetic with generous corner radii.

**Depth:** Uses 5 shadow styles for layering and elevation.

## 2. Color Palette & Roles

- **Dark Purple** (`#1F1633`) — Dark background / footer
- **White** (`#FFFFFF2E`) — Page background
- **Dark Purple** (`#7553FF`) — Accent background
- **Dark Purple** (`#422082`) — Dark background / footer
- **Dark Muted Purple** (`#79628C`) — Secondary background
- **White** (`#FFFFFF`) — Light text (on dark)
- **Light Yellow** (`#DCDCAA`) — Muted text
- **Blue** (`#569CD6`) — Link / accent text
- **Orange** (`#CE9178`) — Muted text
- **Light Muted Blue** (`#E5E7EB`) — Divider / border
- **Dark Muted Purple** (`#584674`) — Divider / border

## 3. Typography Rules

**Primary font:** Dammit Sans
**Secondary font:** Rubik

**Fonts by role:**
- Headings: Dammit Sans, Rubik
- Body: Monaco
- Buttons / nav: Rubik

**All detected fonts:** Rubik (1199), Monaco (1018), IBM Plex Mono (57), Dammit Sans (17)

**Type scale:**
- Headings: 24px, 27px, 30px, 60px, 88px
- Body / UI: 14px, 15px, 16px, 20px
- Captions / Small: 12px

**Weights in use:** 400, 500, 600, 700

**Line heights:** 24px, 32px, 18px, 14px, 66px, 33.75px, 16px, 21px, 30px, 20px

**Letter spacing:** 0.2px, 0%

## 4. Component Stylings

### Buttons
- Background: `rgb(21, 15, 35)`
- Text color: `rgb(255, 255, 255)`
- Padding: 0px 0px 0px 0px
- Font: 16px, weight 400

### Cards
- Shadow: `rgb(21, 15, 35) 0px 0px 8px 6px`
- Corner radius: 4px

### Inputs
- Background: `rgb(66, 32, 130)`
- Border: 0px solid rgb(229, 231, 235)
- Corner radius: 8px
- Padding: 8px 16px 8px 16px
- Font size: 16px

## 5. Layout Principles

**Spacing scale:** 2px, 4px, 8px, 12px, 16px, 20.3px, 32px, 44px

**Base unit:** 4px grid (values are mostly multiples of 4).

**Border radii:** 4px, 8px, 10px, 10.6px, 12px, 13px, 18px, 9999px

## 6. Guidelines

### Do
- Use 4px grid for all spacing
- Use `#7553FF` for primary actions and CTAs
- Stick to 4 font weights: 400, 500, 600, 700
- Use `Dammit Sans` as the primary typeface

### Don't
- Don't introduce colors outside the palette above
- Don't mix fonts beyond Dammit Sans and Rubik
- Don't use inline styles when the design system covers the pattern
- Don't use border-radius values outside: 4px, 8px, 10px, 10.6px, 12px, 13px, 18px, 9999px

---

*This DESIGN.md was generated by [brandmd](https://github.com/yuvrajangadsingh/brandmd). Drop it into your project root and AI coding agents (Claude Code, Cursor, Gemini CLI) will use it to generate on-brand UI.*